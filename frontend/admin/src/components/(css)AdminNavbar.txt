:root {
  --mint-green: #e6fff5;
  --vibrant-green: #22c55e;
  --dark-green: #43A047;
  --sidebar-width: 220px;
  --header-height: 64px;
  --shadow: 0 2px 8px rgba(0,0,0,0.08);
  --card-shadow: 0 8px 20px rgba(17, 121, 205, 0.06);
  --gray-line: #D5D5D5;
}
body, .admin-layout {
  background: #EFFFF1;
  min-height: 100vh;
  margin: 0;
  font-family: 'Inter', Arial, sans-serif;
}

.admin-layout {
  display: flex;
  min-height: calc(100vh - var(--header-height));
}

.admin-sidebar {
  width: var(--sidebar-width);
  background: #fff;
  margin-top: 1.5px;
  border-top-right-radius: 24px;
  display: flex;
  flex-direction: column;
  padding: 32px 0 0 0;
  box-shadow: var(--shadow);
  position: sticky;
  top: var(--header-height);
  align-self: flex-start;
  min-height: calc(100vh - var(--header-height));
  max-height: calc(100vh - var(--header-height));
  overflow-y: auto;
  box-sizing: border-box;
  width: 19%;
}

.admin-main {
  flex: 1;
  display: flex;
  flex-direction: column;
  min-width: 0;
  overflow: auto;
}

.sidebar-logo {
  display: flex;
  align-items: center;
  padding: 0 24px 32px 24px;
  margin-top: -1.2rem;
}

.admin-logo {
  width: 48px;
  height: 48px;
  margin-right: 16px;
  
}

.sidebar-text {
  display: flex;
  flex-direction: column;
  
}

.sidebar-title {
  font-size: 1.15rem;
  font-weight: 600;
  color: #222;
  
  white-space: nowrap;
}

.sidebar-sublabel {
  font-size: 0.95rem;
  color: #888;
  margin-top: -1px;
}

.sidebar-menu {
  display: flex;
  flex-direction: column;
  gap: 8px;
  padding: 15px 16px;
}

.sidebar-menu-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 10px 16px;
  border-radius: 8px;
  cursor: pointer;
  color: #222;
  font-size: 1rem;
  transition: background 0.2s;
}

.sidebar-menu-item:hover {
  background: #43A047;
}

.sidebar-menu-item.active {
  background: var(--dark-green);
  color: #fff;
}

.menu-icon {
  width: 22px;
  height: 22px;
  filter: brightness(0); /* Makes icons black */
}

.sidebar-menu-item.active .menu-icon {
  filter: brightness(0); /* Keep black for active state as well */
}

.admin-main {
  flex: 1;
  display: flex;
  flex-direction: column;
  min-width: 0;
}
.admin-top-row{
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 64px;
    padding: 0 32px;

}
.overview-title-mint {
  font-size: 2rem;
  font-weight: 600;
  color: #222;
  padding: 8px 1px;
}
.top-row-icons{
    display: flex;
    align-items: center;
    gap: 23px;
}
.top-row-icon{
    width: 24px;
    height: 24px;
    cursor: pointer;
    filter: brightness(-0);
}
.admin-top-divider{
    width: 100%;
    height: 1.5px;
    background: var(--gray-line);
    margin-bottom: 20px;
    margin-top: 32px;
}
.admin-top-divider1{
    width: 100%;
    height: 1.5px;
    background: var(--gray-line);
    margin-bottom: 10px;
}
.admin-header {
  height: var(--header-height);
  background: #fff;
  box-shadow: var(--shadow);
  border-bottom: 1px solid var(--gray-line);
  display: flex;
  align-items: center;
  justify-content: flex-end;
  padding:0 32px;
  position: sticky;
  top: 0;
  z-index: 10;
  width: 100vw; /* Full width */
  left: 0;
  margin: 0;
}

.header-icons {
  display: flex;
  align-items: center;
  gap: 24px;
}

.header-icon {
  width: 24px;
  height: 24px;
  cursor: pointer;
  filter: grayscale(100%);
  transition: filter 0.2s;
}

.header-icon:hover {
  filter: none;
}

.admin-content {
  flex: 1;
  padding-left: 30px;
  background: transparent;
  min-height: 0;
}
.metrics-section{
  margin-bottom: 24px;
}
.metrics-header{
  display: flex;
  align-items: center;
  justify-content: flex-start;
  gap: 12px;
  cursor: pointer;
  padding: 8px 4px;
  margin-bottom: 8px;
}
.metrics-header h3{
  margin: 0;
  font-size: 1.5rem;
  font-weight: 600;
}
.chev{
  color: black;
}
.metrics-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(270px, 1fr));
  gap: 8px;
  justify-content: start;
  margin: 0 0 12px 0;
}


.metrics-grid.users-last-row {
  grid-template-columns: repeat(auto-fill, minmax(270px, 1fr));
  justify-content: start;
}

.metric-card {
  display: flex;
  align-items: flex-start;
  gap: 10px;
  background: #fff;
  border-radius: 8px;
  padding: 12px;
  width: 100%;
  max-width: 270px;
  box-shadow: var(--shadow);
  min-height: 70px;
  box-sizing: border-box;
  position: relative;
  overflow: hidden;
}

.circle {
  width: 16px;
  height: 16px;
  border-radius: 50%;
  display: inline-block;
}

.circle.orange {
  background: #FFA600;
}

.circle.teal {
  background: #00E3B6;
}

.circle.blue {
  background: #656DFF;
}

.circle.green {
  background: #00DD31;
}

.metric-title-row {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 6px;
}

.metric-left {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 22px;
  height: 22px;
  flex: 0 0 22px;
}

.metric-left img.small-icon {
  width: 58px;
  height: 58px;
  object-fit: contain;
}

.metric-body {
  display: flex;
  flex-direction: column;
  min-width: 0;
}

.metric-title {
  font-size: 0.95rem;
  color: #3B3B3B;
  margin-bottom: 6px;
  margin-top: 5px;
  font-weight: 500;
}

.metric-number {
  font-weight: 700;
  font-size: 1.25rem;
  color: #111;
}

.metric-subtext {
  font-size: 0.78rem;
  color: #888;
  margin-top: 6px;
}

.metric-divider {
  height: 1.5px;
  background: var(--gray-line);
  display: block;
  margin: 8px -12px;
  width: calc(140% + 25px);
  position: relative;
  left: 0;
}

.metric-card.clickable {
  cursor: pointer;
  transition: transform 0.2s, box-shadow 0.2s;
}

.metric-card.clickable:hover {
  transform: translateY(-1px);
  box-shadow: 0 8px 24px rgba(0,0,0,0.12);
}

/* Responsive adjustments for metrics section */
@media (max-width: 1024px) {
  .metrics-grid {
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    gap: 12px;
  }

  .metric-card {
    max-width: 240px;
    padding: 10px;
  }

  .metric-title {
    font-size: 0.9rem;
  }

  .metric-number {
    font-size: 1.15rem;
  }

  .metric-subtext {
    font-size: 0.75rem;
  }
}

@media (max-width: 768px) {
  .metrics-grid {
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 10px;
  }

  .metrics-grid.users-last-row {
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
  }

  .metric-card {
    max-width: 200px;
    padding: 8px;
    min-height: 60px;
  }

  .metric-left {
    width: 20px;
    height: 20px;
    flex: 0 0 20px;
  }

  .circle {
    width: 10px;
    height: 10px;
  }

  .metric-left img.small-icon {
    width: 50px;
    height: 50px;
  }

  .metric-title {
    font-size: 0.85rem;
  }

  .metric-number {
    font-size: 1.1rem;
  }

  .metric-subtext {
    font-size: 0.7rem;
  }

  .metric-divider {
    margin: 6px -8px;
    width: calc(100% + 17px);
  }
}

@media (max-width: 480px) {
  .metrics-grid {
    grid-template-columns: 1fr;
    gap: 8px;
  }

  .metrics-grid.users-last-row {
    grid-template-columns: 1fr;
  }

  .metric-card {
    max-width: 100%;
    padding: 12px;
    min-height: 70px;
  }

  .metric-left {
    width: 22px;
    height: 22px;
    flex: 0 0 22px;
  }

  .circle {
    width: 12px;
    height: 12px;
  }

  .metric-left img.small-icon {
    width: 58px;
    height: 58px;
  }

  .metric-title {
    font-size: 0.95rem;
  }

  .metric-number {
    font-size: 1.25rem;
  }

  .metric-subtext {
    font-size: 0.78rem;
  }

  .metric-divider {
    margin: 8px -12px;
    width: calc(100% + 25px);
  }
}

/* Responsive adjustments for sidebar */
@media (max-width: 1024px) {
  .admin-sidebar {
    width: 240px; /* Fixed width for tablets */
    padding: 24px 0 0 0;
  }
  .admin-top-divider{
    width: 100%;
    height: 1.5px;
    background: var(--gray-line);
    margin-bottom: 20px;
    margin-top: 16px;
}
  .sidebar-logo {
    padding: 0 20px 24px 20px;
  }

  .sidebar-menu {
    padding: 12px 12px;
  }

  .sidebar-menu-item {
    padding: 8px 12px;
  }
}

@media (max-width: 768px) {
  .admin-sidebar {
    width: 200px; /* Narrower for smaller tablets */
    padding: 20px 0 0 0;
  }
  .admin-top-divider{
    width: 100%;
    height: 1.5px;
    background: var(--gray-line);
    margin-bottom: 20px;
    margin-top: 1.5px;
}
  .sidebar-logo {
    padding: 0 16px 20px 16px;
  }

  .admin-logo {
    width: 40px;
    height: 40px;
    margin-right: 12px;
  }

  .sidebar-title {
    font-size: 1rem;
  }

  .sidebar-sublabel {
    font-size: 0.85rem;
  }

  .sidebar-menu {
    gap: 6px;
    padding: 10px 10px;
  }

  .sidebar-menu-item {
    padding: 8px 10px;
    font-size: 0.9rem;
  }

  .menu-icon {
    width: 20px;
    height: 20px;
  }

  .admin-content {
    padding-left: 20px;
  }
}

@media (max-width: 480px) {
 

  .admin-sidebar.open {
    transform: translateX(0); /* Show when open */
  }

  .sidebar-logo {
    padding: 0 24px 32px 24px;
  }

  .admin-logo {
    width: 48px;
    height: 48px;
    margin-right: 16px;
  }

  .sidebar-title {
    font-size: 1.15rem;
  }

  .sidebar-sublabel {
    font-size: 0.95rem;
  }

  .sidebar-menu {
    gap: 8px;
    padding: 15px 16px;
  }

  .sidebar-menu-item {
    padding: 10px 16px;
    font-size: 1rem;
  }

  .menu-icon {
    width: 22px;
    height: 22px;
  }

  .admin-main {
    padding-left: 0; /* No left padding on mobile */
  }

  .admin-content {
    padding-left: 16px;
    padding-right: 16px;
  }

  .admin-top-row {
    padding: 0 16px;
  }

  .overview-title-mint {
    font-size: 1.5rem;
  }
}
